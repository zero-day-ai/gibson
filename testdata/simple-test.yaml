# Simple Test Workflow
# Tests basic workflow execution with bishop agent

name: simple-test-workflow
description: Basic workflow test to validate orchestrator functionality

nodes:
  - id: test-node
    type: agent
    agent: bishop
    description: Simple test task
    task:
      goal: Perform a basic reconnaissance scan
      context:
        test_mode: true
        max_depth: 1
