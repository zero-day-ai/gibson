# GraphRAG Taxonomy Test Workflow
# Tests all taxonomy node types and relationships

name: graphrag-taxonomy-test
description: Validate GraphRAG taxonomy - node types, relationships, store/query/traverse

nodes:
  - id: graphrag-test
    agent: debug-agent
    context:
      mode: graphrag-test
      verbose: true
    goal: Test GraphRAG taxonomy operations - store nodes, create relationships, query, traverse
