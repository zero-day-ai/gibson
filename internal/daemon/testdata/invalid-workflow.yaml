name: Invalid Test Workflow
description: Intentionally malformed workflow for error testing

# Missing required fields and invalid structure
nodes:
  - id: bad_node
    # Missing 'type' field (required)
    name: Bad Node
    description: This node is intentionally incomplete

  - id: circular_dep_1
    type: agent
    name: Circular Dependency 1
    agent: test-agent
    depends_on:
      - circular_dep_2  # Creates circular dependency

  - id: circular_dep_2
    type: agent
    name: Circular Dependency 2
    agent: test-agent
    depends_on:
      - circular_dep_1  # Creates circular dependency
