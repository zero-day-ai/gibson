# Malformed YAML - intentionally broken syntax
name: Malformed YAML Workflow
description: This file has intentional YAML syntax errors

nodes:
  - id: node1
    type: agent
    name: First Node
    agent: test-agent
    task:
      action: test

  # Missing closing quote on line below
  - id: node2
    type: agent
    name: "Unclosed string
    agent: test-agent

  # Invalid indentation
- id: node3
  type: tool
    tool: bad-indent

  # Invalid YAML map structure
  - id: node4
    type: : agent:
    name:: broken

  # Completely malformed
  - id node5 without colon
    type = agent
    {invalid: structure}
